# Copy to .env and fill in real values. .env is gitignored.

NODE_ENV=development
PORT=5000

# JWT scaffolding. Use a long random string in production.
JWT_SECRET=replace-me-with-a-long-random-string
JWT_EXPIRES_IN=7d

LOG_LEVEL=info
